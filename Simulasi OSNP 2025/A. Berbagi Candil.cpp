#include <bits/stdc++.h>

#define ll long long

using namespace std;

int main(const int argc, char* argv[]) {
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);
	cout.tie(nullptr);
	
	ll m, n;
	cin >> n >> m;
	
	cout << m%n;
	
	return 0;
}
